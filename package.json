{
  "name": "title-bar",
  "author": "lumine-code",
  "version": "1.0.0",
  "description": "Theme-aware custom title bar with integrated menu.",
  "keywords": [
    "frameless",
    "menubar",
    "window-controls",
    "mnemonics",
    "context-menu"
  ],
  "repository": "https://github.com/lumine-code/title-bar",
  "bugs": {
    "url": "https://github.com/lumine-code/title-bar/issues"
  },
  "license": "MIT",
  "main": "./lib/main.js",
  "files": [
    "docs",
    "lib",
    "menus",
    "spec",
    "styles"
  ],
  "engines": {
    "lumine": "^1.0.0"
  },
  "backgroundTips": [
    "{% if keys['title-bar:focus-menu'] %}You can put the keyboard into the application menu with {{ 'title-bar:focus-menu' | keystroke }}{% else %}You can put the keyboard into the application menu by tapping Alt, then reach any item by its underlined letter.{% endif %}",
    "You can right-click anywhere for a context menu drawn and themed like the rest of the editor rather than by the operating system."
  ],
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "configSchema": {
    "controlTheme": {
      "title": "Window Controls Theme",
      "description": "Visual style used for the window control buttons.",
      "type": "string",
      "enum": [
        "Default",
        "Windows 11",
        "macOS Tahoe",
        "GNOME"
      ],
      "default": "Default"
    },
    "customContextMenus": {
      "title": "Custom Context Menus",
      "description": "Use custom styled HTML context menus instead of native Electron menus. Matches the title bar styling.",
      "type": "boolean",
      "default": true
    },
    "autoHide": {
      "title": "Auto Hide Menu Bar",
      "description": "Automatically hide the menu bar and toggle it by pressing Alt.",
      "type": "boolean",
      "default": true
    },
    "altGivesFocus": {
      "title": "Alt Gives Focus",
      "description": "Releasing the alt key gives focus to the application menu.",
      "type": "boolean",
      "default": true
    }
  },
  "providedServices": {
    "title-bar": {
      "versions": {
        "1.0.0": "provideTitleBar"
      }
    }
  },
  "dependencies": {
    "@lumine-code/underscore-plus": "github:lumine-code/underscore-plus#cbf9388917fd1d59a533f43cf68a80d0c48036ac"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.3.0",
    "globals": "^17.11.0",
    "prettier": "^3.9.6"
  }
}
